services:
  mongo:
    image: mongo:7
    container_name: vol404-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: vol404
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
