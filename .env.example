# App
PORT=3000
SESSION_SECRET=change_me_with_a_long_random_value
PUBLIC_BASE_URL=
PROF_SECRET=prof-vol404-2026

# MongoDB
# Generic fallback URI (used first)
MONGO_URI=mongodb://127.0.0.1:27017/vol404

# Optional explicit URIs used by the fallback helper
# Local run (npm start / npm run seed)
MONGO_URI_LOCAL=mongodb://127.0.0.1:27017/vol404
# Docker Compose run (service name on docker network)
MONGO_URI_DOCKER=mongodb://mongo:27017/vol404
